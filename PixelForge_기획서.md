# 🎨 PixelForge — 픽셀아트 변환기 + 에디터 기획서

> 이미지를 픽셀아트로 변환하고 직접 편집해 게임 에셋으로 export하는 웹 애플리케이션

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 목표 | 이미지 → 픽셀아트 자동 변환 + 에디터 편집 → 게임 에셋 export |
| 타겟 | 게임 에셋용 픽셀아트를 제작하려는 개발자 / 아티스트 |
| 플랫폼 | 웹 (브라우저, 데스크탑 전용) |
| 기술 스택 | HTML5 Canvas + Vanilla JavaScript + Web Worker + gif.js |

---

## 🗺️ 전체 워크플로우

```
[1단계] 가져오기       [2단계] 변환          [3단계] 편집           [4단계] 내보내기
━━━━━━━━━━━━━━━━      ━━━━━━━━━━━━━━       ━━━━━━━━━━━━━━━━      ━━━━━━━━━━━━━━
이미지 업로드     →   자동 픽셀아트    →   픽셀 에디터에서   →   게임 엔진용
또는 빈 캔버스        변환 + 후처리         직접 수정              PNG / 스프라이트시트
```

---

## 🖥️ 화면 레이아웃

```
┌──────────────────────────────────────────────────────────────────┐
│  🎨 PixelForge          [변환] [편집] [내보내기]        [💾 저장] │
├────────┬─────────────────────────────────────────┬───────────────┤
│        │                                         │               │
│ 🔧 도구 │           캔버스 (픽셀 에디터)            │  🎨 팔레트    │
│        │                                         │               │
│  ✏️ 연필 │    ┌─────────────────────────┐          │ ■ ■ ■ ■ ■ ■  │
│  ◻ 지우개│   │                         │          │ ■ ■ ■ ■ ■ ■  │
│  🪣 채우기│  │      픽셀 캔버스         │          │ ■ ■ ■ ■ ■ ■  │
│  💧스포이드│ │                         │          │               │
│  ▭ 선   │   │                         │          │ [+ 추가]      │
│  ▭ 사각형│  └─────────────────────────┘          │ [이미지 추출] │
│  ○ 원   │                                         │ [저장/불러오기]│
│  ↕ 이동  │    확대: [──●──] 800%                   │               │
│  ▣ 선택  │    격자: [ON] 배경: [투명▼]              ├───────────────┤
│        │                                         │  🖼️ 레이어    │
│ ─────  │                                         │               │
│        │                                         │ + Layer 2     │
│ 🪞 대칭  │                                         │ ✓ Layer 1     │
│ 🔲 격자  │                                         │ + 배경        │
└────────┴─────────────────────────────────────────┴───────────────┘
│  프레임: [◀] [1/4] [▶]  [+ 추가] [▶️ 재생]   FPS: [──●──] 12   │
└──────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Phase별 기능 명세

### Phase 1 — 변환 엔진

픽셀아트 변환의 핵심 알고리즘 구현

#### 다운샘플링: Sobel 엣지 감지 + 블록 평균 혼합

단순 블록 평균과 달리 이미지의 경계선(엣지)을 먼저 인식한 후 다운샘플링하여 윤곽이 훨씬 선명한 결과물을 생성

```
1. Sobel 필터로 이미지 경계선 감지
2. Otsu's Method로 이미지별 자동 임계값 계산
3. 엣지 방향성을 보존하면서 블록 평균 계산
4. N×N 블록 → 1픽셀로 다운샘플링
```

#### 색상 양자화: LAB 색공간 K-Means++

RGB가 아닌 LAB 색공간에서 색상 거리를 계산 → 인간 시각에 더 자연스러운 팔레트 생성

```
1. RGB → LAB 색공간 변환
2. 전체 픽셀의 10% 랜덤 샘플링 (성능 최적화)
3. K-Means++ 초기화로 수렴 속도 향상
4. 원하는 색상 수의 팔레트 추출
5. 전체 픽셀에 팔레트 매핑
```

#### 디더링: 3종 선택 가능

| 방식 | 속도 | 특징 |
|------|------|------|
| **Bayer (Ordered)** | 빠름 | 규칙적 패턴, 레트로 느낌 |
| **Floyd-Steinberg** | 중간 | 오차를 인접 픽셀로 분산, 자연스러움 |
| **Atkinson** | 중간 | Apple II 스타일, 더 선명함 |

#### 픽셀아트 작가 노하우 적용 (후처리)

실제 픽셀아트 작가들의 규칙을 알고리즘화하여 자동 적용

| 기법 | 설명 |
|------|------|
| **Anti-Aliasing** | 경계 중간색 자동 삽입, 45도 직선/수직수평선 제외 |
| **Selective Outlining** | 광원 방향 기반 외곽선 색상 자동 교체 |
| **Lonely Pixel 제거** | 고립된 픽셀을 주변 색으로 병합 |
| **Banding 방지** | 동일 명도 3개 이상 연속 시 자동 분산 |

#### 배경 투명화

- **마법봉 방식**: 클릭 지점에서 유사 색상 영역 투명화 (Flood Fill 기반)
- **외곽 자동 제거**: 이미지 가장자리에서 안쪽으로 탐색
- 허용 오차(Tolerance) 슬라이더 조절 가능

> ⚠️ 구현 주의: Flood Fill은 재귀 방식 금지 → Queue 기반 반복 방식 사용 (스택 오버플로우 방지)

---

### Phase 2 — 픽셀 에디터

#### 편집 도구

| 도구 | 기능 |
|------|------|
| **연필** | 픽셀 단위 그리기, 굵기 1~4px |
| **지우개** | 픽셀 삭제 (투명화) |
| **채우기** | Flood Fill, 허용 오차 조절 |
| **스포이드** | 캔버스에서 색상 추출 |
| **선** | Bresenham 알고리즘 기반 픽셀 퍼펙트 직선 |
| **사각형 / 원** | 도형 그리기 (채우기 / 외곽선) |
| **선택 영역** | 사각형 선택 → 이동 / 복사 / 삭제 |
| **이동** | 전체 캔버스 내용 이동 |
| **대칭 모드** | 수평 / 수직 대칭 동시 그리기 |

#### 단축키

```
B — 연필       E — 지우개     G — 채우기
I — 스포이드   L — 선         M — 선택
Ctrl+Z — 실행취소 (50단계)
Ctrl+Y — 다시실행
Ctrl+C / V — 복사 / 붙여넣기
[ / ] — 브러시 크기 조절
```

#### Undo / Redo (50단계)

전체 Canvas 상태 저장 대신 **변경된 픽셀만 저장하는 Delta 방식** 채택

```javascript
// Delta 방식 예시
undoStack.push({
  type: 'stroke',
  pixels: [{ x:10, y:20, before:[255,0,0,255], after:[0,255,0,255] }]
});
```

---

### Phase 3 — 레이어 시스템

- 레이어 추가 / 삭제 / 순서 변경 (드래그)
- 레이어별 불투명도 조절
- 레이어 표시 / 숨김 토글
- 레이어 병합 (Merge Down)
- 변환 결과는 자동으로 별도 레이어에 삽입 → 원본 보존
- 변경된 레이어만 재합성하는 **Dirty Flag 방식**으로 성능 최적화

**레이어 활용 예시**
```
Layer 3 — 외곽선 수정 (수작업)
Layer 2 — 색상 수정 (수작업)
Layer 1 — 자동변환 결과 (잠금)
```

---

### Phase 4 — 팔레트 시스템

#### 팔레트 구조

```
팔레트 관리자
├── 프리셋 팔레트 (읽기 전용)
│   ├── 게임보이 (4색)
│   ├── NES (54색)
│   ├── CGA (16색)
│   ├── PICO-8 (16색)
│   └── 흑백 (2색)
├── 내 팔레트 (사용자 저장)
│   ├── 이름 지정 / 수정 / 삭제
│   ├── LocalStorage 영구 저장
│   └── .hex / .pal 파일 가져오기·내보내기
└── 현재 작업 팔레트
    ├── 색상 추가 / 삭제 / 순서 변경
    ├── 색상 클릭 → 컬러피커 (HEX / RGB / HSL)
    └── 이미지에서 자동 추출
```

#### 저장 데이터 구조

```json
{
  "palettes": [
    {
      "id": "uuid-1234",
      "name": "내 첫 팔레트",
      "colors": ["#1a1c2c", "#5d275d", "#b13e53", "#ef7d57"],
      "createdAt": "2026-02-25",
      "isPreset": false
    }
  ]
}
```

#### 호환 포맷

| 포맷 | 설명 |
|------|------|
| `.hex` | 줄마다 HEX 코드, Lospec 표준 |
| `.pal` | RIFF 형식, Aseprite 호환 |
| 클립보드 | HEX 목록 복사/붙여넣기 |

---

### Phase 5 — 애니메이션 & 내보내기

#### 프레임 시스템

- 프레임 추가 / 복제 / 삭제
- 프레임 순서 드래그 변경
- FPS 설정 (1~60)
- 인앱 재생 미리보기
- **어니언 스킨**: 이전 프레임 반투명 표시 (애니메이션 작업 필수)

#### 내보내기 옵션

| 포맷 | 설명 |
|------|------|
| **단일 PNG** | 투명 배경 PNG |
| **스프라이트시트** | 가로 / 세로 / 그리드 배열, Unity / Godot / RPG Maker 호환 |
| **GIF** | 애니메이션 GIF (gif.js 라이브러리) |

---

### Phase 6 — 품질 검사 (자동)

변환 / 편집 후 자동으로 감지하고 경고 표시

| 검사 항목 | 감지 방법 |
|----------|---------|
| 고립 픽셀 (Lonely Pixel) | 주변 4방향 모두 다른 색인 픽셀 탐지 |
| 색상 띠 현상 (Banding) | 동일 명도 픽셀 3개 이상 연속 탐지 |
| 지저분한 선 (Dirty Line) | 외곽선 픽셀 연속성 체크 |
| 색상 초과 | 팔레트 초과 색상 수 표시 |

---

## 🔬 채택 알고리즘 최종 정리

기존의 단순 블록 평균 + Median Cut 방식에서 아래와 같이 업그레이드

| 단계 | 기존 (단순) | 채택 (업그레이드) |
|------|------------|----------------|
| 다운샘플링 | 블록 평균 | Sobel 엣지 감지 + 블록 평균 혼합 |
| 색상 양자화 | Median Cut | LAB 색공간 K-Means++ |
| 디더링 | Floyd-Steinberg 고정 | Floyd-Steinberg / Atkinson / Bayer 선택 |
| Anti-Aliasing | 단순 경계 처리 | 엣지 그래디언트 방향 기반 AA |
| 색상 거리 계산 | RGB 유클리드 | LAB ΔE (인간 시각 기반) |

---

## ⚠️ 구현 리스크 및 해결책

| 기능 | 위험도 | 핵심 해결책 |
|------|--------|------------|
| Canvas 대용량 처리 | 🟡 중간 | Web Worker 분리 (메인 스레드 블로킹 방지) |
| Sobel 임계값 튜닝 | 🟡 중간 | Otsu's Method 자동 임계값 + 수동 슬라이더 병행 |
| LAB K-Means 성능 | 🔴 높음 | 10% 랜덤 샘플링 + K-Means++ 초기화 |
| Floyd-Steinberg 속도 | 🟡 중간 | 순차 처리 필수 (병렬화 불가), 진행 표시바 |
| Undo/Redo 메모리 | 🟡 중간 | Delta 방식 (변경 픽셀만 저장) |
| 레이어 합성 성능 | 🟢 낮음 | Dirty Flag (변경된 레이어만 재합성) |
| GIF 인코딩 | 🟢 낮음 | gif.js 오픈소스 라이브러리 (MIT) |
| LocalStorage 용량 | 🟢 낮음 | 팔레트만 저장, 프로젝트는 .json 파일 다운로드 |
| Flood Fill 스택 오버플로우 | 🟡 중간 | 재귀 금지, Queue 기반 반복 방식 |
| 모바일 지원 | 🔴 높음 | 에디터는 데스크탑 전용, 변환 기능만 모바일 지원 |

---

## 🗓️ 개발 순서

```
Phase 1 — 변환 엔진 (1~2일)
  └ Sobel 엣지 감지, LAB K-Means, 디더링 3종, AA, Sel-Out, 배경 제거
  └ Web Worker로 분리

Phase 2 — 캔버스 & 기본 에디터 (2~3일)
  └ Canvas 렌더링, 연필/지우개/채우기/스포이드
  └ 확대/축소, 격자 표시

Phase 3 — 고급 도구 (1~2일)
  └ 선/도형, 선택영역, 대칭 모드
  └ 단축키 전체 구현, Undo/Redo 50단계 (Delta 방식)

Phase 4 — 레이어 시스템 (1~2일)
  └ 레이어 추가/삭제/순서/불투명도/병합
  └ Dirty Flag 합성 최적화

Phase 5 — 팔레트 시스템 (1일)
  └ 색상 편집 (HEX/RGB/HSL), 이미지 추출
  └ LocalStorage 저장/불러오기, .hex/.pal 가져오기

Phase 6 — 애니메이션 & 내보내기 (2일)
  └ 프레임 시스템, 어니언 스킨, FPS 설정
  └ PNG / 스프라이트시트 / GIF export

Phase 7 — 품질 검사 & UI 다듬기 (1일)
  └ Lonely Pixel, Banding, Dirty Line 자동 감지 및 경고
```

---

## 📦 기술 스택

| 역할 | 기술 |
|------|------|
| 렌더링 | HTML5 Canvas API |
| 로직 | Vanilla JavaScript (순수 JS, 프레임워크 없음) |
| 성능 | Web Worker (무거운 연산 분리) |
| 저장 | LocalStorage (팔레트), .json 파일 (프로젝트) |
| GIF 생성 | gif.js (MIT 라이선스) |
| UI | CSS Grid / Flexbox |

---

## 📁 권장 폴더 구조

```
pixel-forge/
├── CLAUDE.md                  ← Claude Code용 프로젝트 문서
├── index.html
├── src/
│   ├── core/
│   │   ├── converter.js       # 변환 엔진 메인
│   │   ├── editor.js          # 픽셀 에디터 메인
│   │   └── palette.js         # 팔레트 시스템
│   ├── algorithms/
│   │   ├── sobel.js           # Sobel 엣지 감지
│   │   ├── kmeans.js          # LAB K-Means++
│   │   ├── dithering.js       # Floyd-Steinberg / Atkinson / Bayer
│   │   ├── floodfill.js       # 배경 제거 (Queue 방식)
│   │   └── postprocess.js     # AA, Sel-Out, Lonely Pixel, Banding
│   ├── workers/
│   │   └── pixelWorker.js     # Web Worker (무거운 연산)
│   └── ui/
│       ├── canvas.js          # Canvas 렌더링 / 레이어 합성
│       ├── tools.js           # 도구 모음
│       ├── panels.js          # UI 패널 (팔레트, 레이어, 프레임)
│       └── shortcuts.js       # 단축키 관리
└── assets/
    └── palettes/              # 프리셋 팔레트 .hex 파일
```

---

## 🚀 Claude Code 시작 프롬프트 예시

**Phase 1 시작:**
```
CLAUDE.md를 읽고, Phase 1 변환 엔진부터 시작해줘.
src/algorithms/ 폴더에 sobel.js, kmeans.js, dithering.js, floodfill.js, postprocess.js를 구현하고
src/core/converter.js에서 이를 조합해 Web Worker로 실행되도록 해줘.
index.html에서 이미지 업로드 → 변환 → Canvas 미리보기까지 테스트할 수 있게 해줘.
```

**Phase 2 시작:**
```
Phase 1 변환 엔진 위에 픽셀 에디터를 붙여줘.
연필, 지우개, 채우기(Queue 방식 Flood Fill), 스포이드 도구와
확대/축소, 격자 표시, Delta 방식 Undo/Redo 50단계를 구현해줘.
```
